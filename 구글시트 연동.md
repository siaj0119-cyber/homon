# 이지랜딩(EasyLanding) 구글 시트 연동 분석 보고서

본 문서는 현재 이지랜딩(`land_b`) 프로젝트의 구글 시트(Google Sheets) 연동 로직 및 관련 데이터 구조를 분석한 문서입니다.

## 1. 데이터 구조 (Database Schema)

구글 시트 연동을 위한 데이터는 `landing_pages` 테이블의 두 가지 주요 JSONB 컬럼에 저장됩니다.

### 1) `google_tokens`
구글 OAuth 인증 후 발급받은 엑세스 토큰 및 리프레시 토큰 정보를 담고 있습니다.

```json
{
  "access_token": "ya29.a0AfB...",
  "refresh_token": "1//0gA...",
  "expiry_date": 1700000000000,
  "scope": "https://www.googleapis.com/auth/drive.file ...",
  "token_type": "Bearer"
}
```

### 2) `google_sheet_config`
연동할 구글 시트의 설정 정보를 담고 있습니다.

```json
{
  "enabled": true,
  "spreadsheetId": "1BxiMVs0XRYFgwnpsCG_...",
  "sheetId": "0",
  "tabName": "Sheet1" // 기록할 시트 탭 이름
}
```

---

## 2. 연동 로직 (OAuth 인증 흐름)

구글 시트 연동은 OAuth 2.0 프로토콜을 사용하며 아래의 흐름으로 진행됩니다.

1. **인증 요청 (`/api/auth/google`)**
   - 권한 범위(`drive.file`, `userinfo.email`, `userinfo.profile`)를 설정합니다.
   - 백그라운드 갱신을 위해 `access_type='offline'`, `prompt='consent'` 파라미터를 추가하여 구글 로그인 페이지로 리디렉션합니다.

2. **인증 콜백 및 토큰 발급 (`/api/auth/google/callback`)**
   - 구글 로그인 후 콜백 라우트로 반환된 `code`를 구글 서버로 보내 `access_token`과 `refresh_token`을 교환합니다.
   - 발급받은 토큰 정보를 `window.opener.postMessage`를 통해 에디터(부모 창)로 전달합니다.

3. **DB 토큰 저장 (`/api/auth/update-token`)**
   - 클라이언트 측에서 전달받은 토큰을 가지고 이 API를 호출합니다.
   - `landing_pages` 테이블의 해당 페이지 `id`를 찾아 `google_tokens` 컬럼과 활성화된 `google_sheet_config`를 업데이트합니다.

---

## 3. 리드 접수 및 데이터 전송 흐름

사용자가 랜딩페이지에서 폼을 제출하여 리드가 접수(`leads` 테이블)될 때 구글 시트 연동 로직이 작동합니다.

### 1) 리드 접수 트리거 (`/api/leads`)
- 사용자의 폼 제출 정보, UTM 정보 등을 검증하고 안전하게 Supabase Admin 클라이언트를 사용하여 DB(`leads` 테이블)에 저장합니다.
- DB 저장이 완료되면 백그라운드 비동기 함수인 `runAlarms()`가 실행됩니다 (Cloudflare `waitUntil` 활용).
- `runAlarms()` 내부에서 페이지의 `google_sheet_config.enabled`가 `true`인지 확인하고 `appendToSheet` 헬퍼 함수를 호출합니다.

### 2) 구글 시트 데이터 쓰기 (`src/services/thirdParty/googleSheet.ts`)
데이터를 구글 시트에 기록하는 핵심 비즈니스 로직입니다.

1. **토큰 갱신 체크 (`forceRefreshToken`)**
   - `tokens.expiry_date`를 확인하여 토큰이 만료되었거나, 만료 1분 전이라면 `refresh_token`을 사용하여 구글에서 새로운 토큰을 발급받습니다.
   - 발급받은 새 토큰 정보는 DB의 `google_tokens`에 즉시 업데이트합니다.

2. **기록할 데이터 조합**
   - **동적 헤더/데이터 생성**: 에디터에서 설정한 입력 폼(`formFields`)을 기준으로 헤더와 데이터를 매핑합니다.
   - 기본적으로 신청일시, 페이지 이름, 플랫폼, URL, 동의 여부 및 폼 데이터와 IP 주소, UTM 메타데이터 등을 포함시킵니다.

3. **헤더 존재 여부 확인 (API 1차 호출)**
   - 구글 시트 API를 통해 `범위!A1:Z1`를 읽어들여 1행에 헤더가 존재하는지 확인합니다.
   - 헤더가 존재하지 않는 새로운 시트의 경우, 조합해둔 헤더 배열을 시트 1행에 새롭게 추가(`append`)합니다.

4. **데이터 기록 (API 2차 호출)**
   - 조합된 사용자 리드 데이터를 시트의 마지막 행에 추가(`append`)합니다.
   - 401(권한 없음) 에러 발생 시, 토큰 갱신 로직(`forceRefreshToken`)을 재실행 후 1회 한정으로 재시도를 진행하여 안정성을 높입니다.

---

## 4. 요약

*   **OAuth 인증**: `Google OAuth 2.0` (Offline Access 모드로 Refresh Token 발급).
*   **DB 스키마**: `landing_pages` 테이블의 JSONB 형식 데이터(`google_tokens`, `google_sheet_config`).
*   **전송 방식**: 랜딩페이지에서 DB에 리드를 Insert한 직후, Cloudflare Worker의 백그라운드 비동기 처리(`waitUntil`)를 통해 `구글 Sheets API (v4)`로 행을 Append 합니다.
*   **오류 복구**: 토큰 만료 시 Refresh Token으로 자동 갱신하고 DB를 업데이트하여 토큰 수명을 연장합니다.
